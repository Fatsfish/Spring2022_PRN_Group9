@page
@model EMS.Pages.UserRole.EditModel

@{
    ViewData["Title"] = "Edit";
}

<h1>Edit</h1>

<h4>UserRole</h4>
<hr />
<div class="row">
    <div class="col-md-4">
        <form method="post">
            <div asp-validation-summary="ModelOnly" class="text-danger"></div>
            <div class="form-group">
                <label asp-for="UserRole.UserId" class="control-label"></label>
                <select asp-for="UserRole.UserId" class="form-control" asp-items="ViewBag.UserId"></select>
                <span asp-validation-for="UserRole.UserId" class="text-danger"></span>
            </div>
            <div class="form-group">
                <label asp-for="UserRole.RoleId" class="control-label"></label>
                <select asp-for="UserRole.RoleId" class="form-control" asp-items="ViewBag.RoleId"></select>
                <span asp-validation-for="UserRole.RoleId" class="text-danger"></span>
            </div>
            <input type="hidden" asp-for="UserRole.Id" />
            <div class="form-group">
                <input type="submit" value="Save" class="btn btn-primary" />
            </div>
        </form>
    </div>
</div>

<div>
    <a asp-page="./Index">Back to List</a>
</div>

@section Scripts {
    @{await Html.RenderPartialAsync("_ValidationScriptsPartial");}
}
